name = "Multi-app workflow"
prompt = "Take a screenshot of Safari, then open Calculator and calculate 2+2"

[expect]
valid = true
min_nodes = 5
max_nodes = 10
required_tools = ["take_screenshot", "click"]
